# Database configuration
# SQLite (development):
DATABASE_URL=sqlite:///instance/school_schedule.db
# PostgreSQL:
# DATABASE_URL=postgresql://postgres:your_password@localhost:5432/school_schedule

# Development: from the repo root — npm install && npm run dev
# (starts FastAPI :8000 and Vite :5173; see README)
